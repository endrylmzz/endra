# ENDRA Web / PWA (reserved)

Not implemented yet. Planned for Phase 8 (see `docs/TASKS.yaml`, `WEB-*` tasks).

The web app will be a thin client over ENDRA Core - it must not contain its
own agent logic, memory handling, or tool execution. All of that lives in
`apps/core`.
