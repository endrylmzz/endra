# ENDRA Persona Config (reserved)

Not implemented yet - built in Phase 1 (`CORE-007`, see `docs/TASKS.yaml`).

Persona (system prompt, tone, behavioral rules) will live here as config, not
hardcoded inline in Core or channel code. See `docs/ARCHITECTURE.md` and
CLAUDE.md section on ENDRA's identity for the intended character.
